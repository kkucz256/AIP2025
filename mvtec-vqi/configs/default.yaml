seed: 42
data_dir: data/mvtec_ad
artifacts_dir: artifacts
reports_dir: reports
category: bottle
image_size: 256
batch_size: 8
num_workers: 2
padim:
  selected_channels: 100
  gaussian_kernel: 5
  blur_sigma: 1.0
cae:
  base_channels: 32
  latent_channels: 256
  learning_rate: 0.001
  weight_decay: 0.0
  batch_size: 16
  epochs: 30
  use_amp: true
  loss_mse_weight: 0.85
  loss_ssim_weight: 0.15
  score_percentile: 0.9
evaluation:
  percentile_threshold: 0.9
infer:
  threshold: 0.55
  score_percentile: 0.9
  save_overlay: true
  overlay_dir: reports/overlays_cli
  use_amp: auto
